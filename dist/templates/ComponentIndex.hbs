{{#if styled}}
import styled from "styled-components";
{{/if}}
{{#if connect}}
import { connect, MapStateToPropsParam, MapDispatchToPropsParam } from "react-redux";
{{/if}}
import {{name}} from "./{{name}}"
{{#if styled}}
    import styles from "./styles";
{{/if}}

{{#if connect}}
const mapStateToProps: MapStateToPropsParam<{{name}}StateProps, {{name}}OwnProps, {{rootState}}> = (state, ownProps) => ({
});

const mapDispatchToProps: MapDispatchToPropsParam<{{name}}DispatchProps, {{name}}OwnProps> = (dispatch) => ({
});
{{/if}}

export default
    {{#if styled}}
        styled(
    {{/if}}
    {{#if connect}}
    connect<{{name}}StateProps, {{name}}DispatchProps, {{name}}OwnProps, {{rootState}}>(
        mapStateToProps,
        mapDispatchToProps
    )(
    {{/if}}
    {{name}}
    {{#if connect}}
    )
    {{/if}}
    {{#if styled}}
    )`
        ${styles}
    `
    {{/if}}
