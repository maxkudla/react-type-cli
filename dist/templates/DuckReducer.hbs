import {Reducer, Action} from "redux";

export const {{name}}Start: Reducer<{{Capitalize duckName}}State, Action> = (state, action) => ({
    ...state
});

export const {{name}}Success: Reducer<{{Capitalize duckName}}State, Action> = (state, action) => ({
    ...state
});

export const {{name}}Fail: Reducer<{{Capitalize duckName}}State, Action> = (state, action) => ({
    ...state,
});
