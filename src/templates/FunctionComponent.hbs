import React from "react";

const {{name}}: React.FC<{{name}}Props> = (props) => {
    return (
        <></>
    )
}

export default
{{#if memo}}
    React.memo(
{{/if}}
    {{name}}
{{#if memo}}
    )
{{/if}}
